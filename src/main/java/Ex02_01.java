public class Ex02_01 {
    public static void main (String[]args){
        Turtle to = new Turtle(); // 클래스 클래스명 = 생성자 클래스();
        // A클래스를 가지고 와서 B라는 이름으로 만든다 객체화/인스턴스화
        to.forward(200);
        to.left(90);
        to.forward(200);
        to.left(90);
        to.forward(200);
        to.left(90);
        to.forward(200);
    }
}
